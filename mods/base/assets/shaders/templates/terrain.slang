// Heartstead terrain material template, runtime contract v3.
//
// Material prototypes select this template and provide data through the documented terrain
// bindings. The production GLSL is compiled from engine/renderer/shaders/builtin/terrain.* because
// all terrain materials intentionally share one pipeline; this file is the stable authoring
// template identity used by cooked material records, not a per-material shader permutation.
